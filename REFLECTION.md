# REFLECTION

AI进行了辅助润色与补充项目细节。

## 1. 哪些 Superpowers 技能作用最大？

- **brainstorming**：帮我确定了项目方向——治理优先的 coding agent harness，也提出很多我之前没想到的细节（HITL 与 WebUI 审批台的结合、凭据威胁模型、危险动作分级）。它一次只问一个问题、逐节确认、未获批不写代码，让项目骨架立得稳，SPEC 落稿前几乎没有返工。
- **writing-plans**：把 T01–T20 拆成"红测先行"的任务，标注依赖与并行批次——**测试代码即规约**，实现者照着测试写就行，歧义大幅减少。冷启动时第二个 agent 只凭 SPEC+PLAN 就独立实现了两个任务，说明规约清晰度是够的。

## 2. 哪些形式大于实质？

- **using-git-worktrees**：因为本机沙箱写权限限制，worktree 无法落在工作区之外，最终改用"每 task 独立分支 + 两阶段评审 + 合并"，只保留了形式上的隔离语义（已在 AGENT_LOG T007 记录偏离）。流程的"形"守住了，但"隔离工作区"这件事实际没发生。

## 3. TDD 是阻碍还是放大器？

- 我认为是**放大器**。本项目是纯后端/机制型，没有 UI，行为可确定性判定，非常适合 TDD：先把边界情况写成测试用例，再让 AI 实现，**跑一遍测试就知道对不对**，不需要逐行人工 review，人工精力可以放在规格与架构评审上。
- 它也真的抓出了 bug：HITL 跨实例状态陈旧读、模型答案被丢弃（`final_output` 为空）、多动作审批撞 id 崩溃、命令护栏子串误杀——全是"先红、实现、后绿"的节奏里暴露并锁定的。
- 局限也要承认：mock 全绿不等于真实可用。真实模型不按 JSON 协议输出、单次回答会被截断，这些 TDD 测不出来，必须补真实链路演示（AGENT_LOG T026）。所以 TDD 放大的是"确定性机制"的质量，替代不了"真实世界验证"。

## 4. subagent 工作流能自主运行多久？最优颗粒度？

- 实测：subagent 完成一个 task 后就丢了上下文；派发消息约半数丢失；有一次它越权一口气实现了 T03–T05，最后我用主 agent 把它拉回来。**单次任务边界必须写死**：做哪个 task、禁止合并、禁止越权、禁止改日志。
- 我的 PLAN 每个 task 约 20 分钟。结论：**颗粒度宁小勿大**，相似的小任务可以合并，但单个 task（AI 实现 + 人工审核）最好不超过 30 分钟；宁可多拆几个 task，也不要一个大 task——大 task 一跑偏，返工成本远高于拆分成本。

## 5. SPEC/PLAN 质量如何影响实现质量？

- **必须写清楚，否则 AI 会跑偏**。最典型的例子：冷启动验证时 PLAN 写了 `requires-python <3.14`，但开发机是 3.14，第二个 agent 直接卡在安装上——**规约与环境的矛盾，只有真跑一遍才暴露**。这次教训让我把版本策略从"预先锁定"改成"验证驱动"。
- 有效的做法：**把测试代码直接写进 task 作为规约**，实现者"红→绿"即完成；PLAN 里 CLI 解析测试曾因缺必填参数在实现时才被发现，说明规约本身也要被测试。

## 6. 最有效的 prompt / context 策略？

- 任务四要素：目标 / 文件路径 / 验收标准 / 要求跑测试。给"生成高精度加法程序"这类任务时，四要素齐全就一次成功。
- 工具协议用**解析器代码强制**而不是提示词自觉：JSON 动作 + 拒绝代码块 + 兼容 XML 工具调用 + 原生 tool_calls（AGENT_LOG T026）。真实模型不听话时，加"行为约束"描述（list_dir 一次→每文件只读一次→禁 shell→直接输出）就明显变稳。
- 真实模型单次回答长度不稳定，长文生成要靠工程兜底：给 harness 加 `append_file` 工具让模型分块写文件，而不是赌一次输出。

## 7. 凭据与分发迫使你想清了什么？

- 做 `cah key set` 时才发现 **key 可能泄漏的途径这么多**：源码、git 历史、日志、终端历史、CI 配置、进程环境——所以 keyring 优先、`.env` 明示明文风险、CI 走 secrets、日志脱敏，哪一条都不能省。
- 做分发（PyPI wheel / Dockerfile / 部署）时发现：**让别人跑起来比自己跑起来麻烦得多**——平台选型、是否免卡免费、冷启动、版本约束、依赖安装全都要提前想。部署平台换了两轮（Render/HF 需绑卡，最后落到 Streamlit），这类约束应该更早进入设计。

## 8. 如果重做，会改变什么？

- **先找好免费部署平台再定 SPEC**：部署形态会影响配置、打包与验收标准，不该等到最后才定。
- 一开始就实现原生工具调用，不走"纯文本 JSON 协议"再补 XML 解析的弯路；尽早做真实 LLM 演示，避免"mock 全绿但真实不可用"。
- subagent 指令从第一天就写明"不得合并、不得越权、不得改日志"，少交一次流程失控的学费。

## 9. 对 Superpowers 方法论的批判

- 它假设我能严格遵循 TDD、worktree 隔离、subagent 单次任务——**这些假设在我的环境里并没有成立**：worktree 被沙箱写权限限制、subagent 丢消息且越权、部分流程被迫由主 agent 内联执行。
- 但结论不是"方法论无用"：它守住了纪律（先规约、先红测、先验证），真正需要人来回答的仍然是"做什么"和"做对了吗"。当 LLM 能完成大部分思考时，工程师的价值恰恰在于用确定性工程（护栏、反馈、预算、验证）把不可靠封装成可靠——这既是本项目的主题，也是我对这套方法论最核心的认同。
