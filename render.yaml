services:
  - type: web
    name: cah-demo
    runtime: python
    buildCommand: pip install -e ".[dev]"
    startCommand: uvicorn cah.web.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: HARNESS_DEMO
        value: "1"
      - key: CAH_STORE_DIR
        value: "/var/data/cah"
